"use client";

/**
 * Fremont Construction & Contracting — Landing Page
 * Next.js App Router · Tailwind CSS · Lucide React · TypeScript
 */

import { useState } from "react";
import {
  Menu,
  X,
  Home,
  Layers,
  Wind,
  Hammer,
  Shield,
  Award,
  DollarSign,
  MapPin,
  Phone,
  Mail,
  ChevronRight,
  CheckCircle,
} from "lucide-react";

// ─── Types ────────────────────────────────────────────────────────────────────

interface Service {
  icon: React.ReactNode;
  title: string;
  description: string;
  features: string[];
}

interface WhyUsItem {
  icon: React.ReactNode;
  title: string;
  description: string;
}

// ─── Static Data ──────────────────────────────────────────────────────────────

const services: Service[] = [
  {
    icon: <Home className="w-8 h-8" />,
    title: "Residential Exterior Construction",
    description:
      "Complete exterior builds and renovations designed for lasting curb appeal and structural integrity.",
    features: [
      "New construction builds",
      "Full exterior renovations",
      "Structural repairs",
      "Code-compliant work",
    ],
  },
  {
    icon: <Layers className="w-8 h-8" />,
    title: "Roofing & Shingle Replacement",
    description:
      "From complete roof replacements to targeted repairs, we protect your home from the elements.",
    features: [
      "Asphalt & architectural shingles",
      "Full tear-off & replacement",
      "Leak detection & repair",
      "Storm damage restoration",
    ],
  },
  {
    icon: <Wind className="w-8 h-8" />,
    title: "Vinyl Siding Installation",
    description:
      "Premium vinyl siding solutions that boost curb appeal while improving your home's energy efficiency.",
    features: [
      "Premium vinyl products",
      "Expert installation",
      "Color & style variety",
      "Low-maintenance finish",
    ],
  },
  {
    icon: <Hammer className="w-8 h-8" />,
    title: "Custom Multi-Tiered Decks",
    description:
      "Handcrafted custom decks built to your vision — from single-level platforms to elaborate multi-tiered retreats.",
    features: [
      "Custom designs",
      "Composite & wood options",
      "Multi-level builds",
      "Built to local codes",
    ],
  },
];

const whyUsItems: WhyUsItem[] = [
  {
    icon: <Shield className="w-7 h-7" />,
    title: "Safety First",
    description:
      "Rigorous on-site safety protocols protect our crew, your family, and your property throughout every project.",
  },
  {
    icon: <Award className="w-7 h-7" />,
    title: "Fully Licensed & Insured",
    description:
      "We carry comprehensive liability and worker's comp insurance — giving you complete peace of mind.",
  },
  {
    icon: <DollarSign className="w-7 h-7" />,
    title: "Transparent Pricing",
    description:
      "No surprise charges. Detailed, itemized estimates upfront so you always know exactly what you're paying for.",
  },
  {
    icon: <MapPin className="w-7 h-7" />,
    title: "Local Expertise",
    description:
      "As a locally owned business, we understand the regional climate, building codes, and community standards.",
  },
];

const navLinks = [
  { label: "Services", href: "#services" },
  { label: "Why Us", href: "#why-us" },
  { label: "Contact", href: "#contact" },
];

// ─── Navbar ───────────────────────────────────────────────────────────────────

function Navbar() {
  const [mobileOpen, setMobileOpen] = useState(false);

  return (
    <header className="fixed top-0 left-0 right-0 z-50 bg-slate-900/95 backdrop-blur-sm border-b border-slate-700/50 shadow-lg">
      <nav className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex items-center justify-between h-16">

          {/* Brand */}
          <a href="#" className="flex items-center gap-2.5">
            <div className="w-8 h-8 bg-amber-500 rounded flex items-center justify-center flex-shrink-0">
              <Hammer className="w-5 h-5 text-slate-900" />
            </div>
            <span className="text-white font-bold text-base sm:text-lg leading-tight">
              Fremont{" "}
              <span className="text-amber-400">Construction</span>
            </span>
          </a>

          {/* Desktop Navigation */}
          <div className="hidden md:flex items-center gap-8">
            {navLinks.map((link) => (
              <a
                key={link.label}
                href={link.href}
                className="text-slate-300 hover:text-amber-400 transition-colors duration-200 text-sm font-medium"
              >
                {link.label}
              </a>
            ))}
            <a
              href="#contact"
              className="bg-amber-500 hover:bg-amber-400 active:bg-amber-600 text-slate-900 font-bold text-sm px-5 py-2.5 rounded transition-colors duration-200"
            >
              Get an Estimate
            </a>
          </div>

          {/* Mobile Hamburger */}
          <button
            onClick={() => setMobileOpen((prev) => !prev)}
            className="md:hidden text-slate-300 hover:text-white transition-colors p-1"
            aria-label="Toggle navigation menu"
            aria-expanded={mobileOpen}
          >
            {mobileOpen ? <X className="w-6 h-6" /> : <Menu className="w-6 h-6" />}
          </button>
        </div>

        {/* Mobile Dropdown */}
        {mobileOpen && (
          <div className="md:hidden border-t border-slate-700 py-4 flex flex-col gap-3">
            {navLinks.map((link) => (
              <a
                key={link.label}
                href={link.href}
                onClick={() => setMobileOpen(false)}
                className="text-slate-300 hover:text-amber-400 transition-colors font-medium px-2 py-1"
              >
                {link.label}
              </a>
            ))}
            <a
              href="#contact"
              onClick={() => setMobileOpen(false)}
              className="mt-1 bg-amber-500 hover:bg-amber-400 text-slate-900 font-bold text-sm px-5 py-3 rounded text-center transition-colors duration-200"
            >
              Get an Estimate
            </a>
          </div>
        )}
      </nav>
    </header>
  );
}

// ─── Hero ─────────────────────────────────────────────────────────────────────

function Hero() {
  return (
    <section className="relative min-h-screen flex items-center justify-center overflow-hidden bg-slate-900">

      {/* Diagonal stripe texture */}
      <div
        aria-hidden="true"
        className="absolute inset-0 opacity-[0.04]"
        style={{
          backgroundImage: `repeating-linear-gradient(
            -45deg,
            #f59e0b 0px,
            #f59e0b 1px,
            transparent 1px,
            transparent 48px
          )`,
        }}
      />

      {/* Radial gradient overlay */}
      <div
        aria-hidden="true"
        className="absolute inset-0 bg-gradient-to-br from-slate-900 via-slate-800/90 to-slate-900"
      />

      {/* Ambient glow blobs */}
      <div
        aria-hidden="true"
        className="absolute top-1/4 right-0 w-[500px] h-[500px] bg-amber-500/8 rounded-full blur-3xl pointer-events-none"
      />
      <div
        aria-hidden="true"
        className="absolute bottom-0 left-0 w-[400px] h-[400px] bg-amber-600/6 rounded-full blur-3xl pointer-events-none"
      />

      {/* Fade to next section */}
      <div
        aria-hidden="true"
        className="absolute bottom-0 left-0 right-0 h-28 bg-gradient-to-t from-slate-50 to-transparent"
      />

      {/* Content */}
      <div className="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center pt-20 pb-16">

        {/* Trust badge */}
        <div className="inline-flex items-center gap-2 bg-amber-500/10 border border-amber-500/25 text-amber-400 text-sm font-semibold px-4 py-2 rounded-full mb-8">
          <CheckCircle className="w-4 h-4 flex-shrink-0" />
          Licensed & Insured General Contractor
        </div>

        {/* Headline */}
        <h1 className="text-5xl sm:text-6xl md:text-7xl font-extrabold text-white leading-[1.08] tracking-tight mb-6">
          Quality Craftsmanship
          <br />
          <span className="text-amber-400">From Foundation</span>
          <br />
          to Finish
        </h1>

        {/* Subheadline */}
        <p className="text-slate-300 text-lg sm:text-xl max-w-2xl mx-auto mb-10 leading-relaxed">
          Fremont Construction & Contracting specializes in residential exterior
          projects — roofing, siding, and custom decks — built to last and priced
          with total transparency.
        </p>

        {/* CTA Buttons */}
        <div className="flex flex-col sm:flex-row gap-4 justify-center">
          <a
            href="#services"
            className="bg-amber-500 hover:bg-amber-400 active:bg-amber-600 text-slate-900 font-bold text-base px-8 py-4 rounded transition-all duration-200 hover:shadow-xl hover:shadow-amber-500/20 hover:-translate-y-0.5"
          >
            View Our Services
          </a>
          <a
            href="#contact"
            className="border-2 border-slate-500 hover:border-amber-400 text-slate-200 hover:text-amber-400 font-bold text-base px-8 py-4 rounded transition-all duration-200 hover:-translate-y-0.5"
          >
            Request a Quote
          </a>
        </div>

        {/* Social proof stats */}
        <div className="mt-20 grid grid-cols-3 gap-6 max-w-sm mx-auto">
          {[
            { value: "15+", label: "Years Experience" },
            { value: "500+", label: "Projects Done" },
            { value: "100%", label: "Satisfaction" },
          ].map((stat) => (
            <div key={stat.label}>
              <div className="text-3xl font-extrabold text-amber-400 tabular-nums">
                {stat.value}
              </div>
              <div className="text-slate-500 text-xs mt-1 uppercase tracking-widest leading-tight">
                {stat.label}
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}

// ─── Services Grid ────────────────────────────────────────────────────────────

function Services() {
  return (
    <section id="services" className="py-24 bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

        {/* Header */}
        <div className="text-center mb-16">
          <span className="text-amber-600 font-bold uppercase tracking-widest text-sm">
            What We Do
          </span>
          <h2 className="mt-3 text-4xl font-extrabold text-slate-900 tracking-tight">
            Our Core Services
          </h2>
          <p className="mt-4 text-slate-600 text-lg max-w-2xl mx-auto leading-relaxed">
            From the roof down to the deck, we handle every aspect of your
            home's exterior with precision and care.
          </p>
        </div>

        {/* Cards */}
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
          {services.map((service) => (
            <div
              key={service.title}
              className="group bg-white rounded-xl p-6 shadow-sm border border-slate-200 hover:border-amber-400 hover:shadow-xl hover:-translate-y-1.5 transition-all duration-300"
            >
              {/* Icon */}
              <div className="w-14 h-14 bg-amber-50 group-hover:bg-amber-500 rounded-xl flex items-center justify-center text-amber-500 group-hover:text-white transition-all duration-300 mb-5">
                {service.icon}
              </div>

              <h3 className="text-slate-900 font-bold text-lg mb-2 group-hover:text-amber-600 transition-colors duration-200 leading-snug">
                {service.title}
              </h3>
              <p className="text-slate-500 text-sm leading-relaxed mb-4">
                {service.description}
              </p>

              <ul className="space-y-1.5">
                {service.features.map((feature) => (
                  <li
                    key={feature}
                    className="flex items-center gap-2 text-sm text-slate-600"
                  >
                    <ChevronRight className="w-3.5 h-3.5 text-amber-500 flex-shrink-0" />
                    {feature}
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}

// ─── Why Choose Us ────────────────────────────────────────────────────────────

function WhyChooseUs() {
  return (
    <section id="why-us" className="py-24 bg-slate-900 relative overflow-hidden">

      {/* Dot grid texture */}
      <div
        aria-hidden="true"
        className="absolute inset-0 opacity-[0.07]"
        style={{
          backgroundImage: `radial-gradient(circle, #f59e0b 1px, transparent 1px)`,
          backgroundSize: "28px 28px",
        }}
      />

      <div className="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

        {/* Header */}
        <div className="text-center mb-16">
          <span className="text-amber-400 font-bold uppercase tracking-widest text-sm">
            The Fremont Difference
          </span>
          <h2 className="mt-3 text-4xl font-extrabold text-white tracking-tight">
            Why Choose Us
          </h2>
          <p className="mt-4 text-slate-400 text-lg max-w-2xl mx-auto leading-relaxed">
            We don't just build structures — we build relationships founded on
            trust, quality, and accountability.
          </p>
        </div>

        {/* Feature Cards */}
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
          {whyUsItems.map((item) => (
            <div
              key={item.title}
              className="group text-center p-8 rounded-xl border border-slate-700 hover:border-amber-500/60 bg-slate-800/40 hover:bg-slate-800 transition-all duration-300 hover:-translate-y-1"
            >
              <div className="w-14 h-14 bg-amber-500/10 group-hover:bg-amber-500 rounded-full flex items-center justify-center text-amber-400 group-hover:text-slate-900 transition-all duration-300 mx-auto mb-5">
                {item.icon}
              </div>
              <h3 className="text-white font-bold text-lg mb-2">{item.title}</h3>
              <p className="text-slate-400 text-sm leading-relaxed">
                {item.description}
              </p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}

// ─── Contact / CTA ────────────────────────────────────────────────────────────

function Contact() {
  return (
    <section id="contact" className="py-24 bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-16 items-start">

          {/* Left column — info */}
          <div>
            <span className="text-amber-600 font-bold uppercase tracking-widest text-sm">
              Get In Touch
            </span>
            <h2 className="mt-3 text-4xl font-extrabold text-slate-900 leading-tight tracking-tight">
              Ready to Start
              <br />
              Your Project?
            </h2>
            <p className="mt-4 text-slate-600 text-lg leading-relaxed">
              Fill out the form and we'll get back to you within one business
              day with a free, no-obligation estimate tailored to your project.
            </p>

            <div className="mt-10 space-y-5">
              {[
                {
                  icon: <Phone className="w-5 h-5" />,
                  label: "Phone",
                  value: "(555) 123-4567",
                },
                {
                  icon: <Mail className="w-5 h-5" />,
                  label: "Email",
                  value: "info@fremontcontracting.com",
                },
                {
                  icon: <MapPin className="w-5 h-5" />,
                  label: "Service Area",
                  value: "Fremont & Surrounding Counties",
                },
              ].map((item) => (
                <div key={item.label} className="flex items-center gap-4">
                  <div className="w-10 h-10 bg-amber-50 rounded-lg flex items-center justify-center text-amber-600 flex-shrink-0">
                    {item.icon}
                  </div>
                  <div>
                    <div className="text-xs text-slate-400 uppercase font-semibold tracking-wider">
                      {item.label}
                    </div>
                    <div className="text-slate-800 font-semibold">{item.value}</div>
                  </div>
                </div>
              ))}
            </div>

            {/* Guarantee note */}
            <div className="mt-10 p-5 bg-amber-50 border border-amber-200 rounded-xl">
              <div className="flex items-start gap-3">
                <CheckCircle className="w-5 h-5 text-amber-600 flex-shrink-0 mt-0.5" />
                <p className="text-slate-700 text-sm leading-relaxed">
                  <strong>Our promise:</strong> Every estimate is free, detailed,
                  and comes with zero pressure. We earn your business by being
                  the best — not by locking you in.
                </p>
              </div>
            </div>
          </div>

          {/* Right column — form */}
          <div className="bg-slate-50 rounded-2xl p-8 border border-slate-200 shadow-sm">
            <h3 className="text-xl font-bold text-slate-900 mb-6">
              Request a Free Estimate
            </h3>

            <form
              className="space-y-5"
              onSubmit={(e) => e.preventDefault()}
              noValidate
            >
              {/* Name + Phone row */}
              <div className="grid grid-cols-1 sm:grid-cols-2 gap-5">
                <div>
                  <label
                    htmlFor="name"
                    className="block text-sm font-semibold text-slate-700 mb-1.5"
                  >
                    Full Name <span className="text-amber-500">*</span>
                  </label>
                  <input
                    id="name"
                    type="text"
                    placeholder="John Smith"
                    required
                    className="w-full px-4 py-3 rounded-lg border border-slate-300 focus:border-amber-500 focus:ring-2 focus:ring-amber-500/20 outline-none transition-all text-slate-900 placeholder:text-slate-400 bg-white text-sm"
                  />
                </div>
                <div>
                  <label
                    htmlFor="phone"
                    className="block text-sm font-semibold text-slate-700 mb-1.5"
                  >
                    Phone Number
                  </label>
                  <input
                    id="phone"
                    type="tel"
                    placeholder="(555) 000-0000"
                    className="w-full px-4 py-3 rounded-lg border border-slate-300 focus:border-amber-500 focus:ring-2 focus:ring-amber-500/20 outline-none transition-all text-slate-900 placeholder:text-slate-400 bg-white text-sm"
                  />
                </div>
              </div>

              {/* Email */}
              <div>
                <label
                  htmlFor="email"
                  className="block text-sm font-semibold text-slate-700 mb-1.5"
                >
                  Email Address <span className="text-amber-500">*</span>
                </label>
                <input
                  id="email"
                  type="email"
                  placeholder="john@example.com"
                  required
                  className="w-full px-4 py-3 rounded-lg border border-slate-300 focus:border-amber-500 focus:ring-2 focus:ring-amber-500/20 outline-none transition-all text-slate-900 placeholder:text-slate-400 bg-white text-sm"
                />
              </div>

              {/* Project description */}
              <div>
                <label
                  htmlFor="project"
                  className="block text-sm font-semibold text-slate-700 mb-1.5"
                >
                  Project Description
                </label>
                <textarea
                  id="project"
                  rows={5}
                  placeholder="Tell us about your project — type of work, timeline, approximate scope..."
                  className="w-full px-4 py-3 rounded-lg border border-slate-300 focus:border-amber-500 focus:ring-2 focus:ring-amber-500/20 outline-none transition-all text-slate-900 placeholder:text-slate-400 bg-white text-sm resize-none"
                />
              </div>

              <button
                type="submit"
                className="w-full bg-amber-500 hover:bg-amber-400 active:bg-amber-600 text-slate-900 font-bold py-4 rounded-lg transition-all duration-200 hover:shadow-xl hover:shadow-amber-500/25 text-base"
              >
                Send My Request
              </button>

              <p className="text-xs text-slate-400 text-center">
                We respond within 1 business day. No spam, ever.
              </p>
            </form>
          </div>
        </div>
      </div>
    </section>
  );
}

// ─── Footer ───────────────────────────────────────────────────────────────────

function Footer() {
  const year = new Date().getFullYear();

  return (
    <footer className="bg-slate-900 border-t border-slate-700/50 pt-14 pb-8">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="grid grid-cols-1 md:grid-cols-3 gap-10 pb-10 border-b border-slate-700/50">

          {/* Brand column */}
          <div>
            <div className="flex items-center gap-2.5 mb-4">
              <div className="w-8 h-8 bg-amber-500 rounded flex items-center justify-center flex-shrink-0">
                <Hammer className="w-5 h-5 text-slate-900" />
              </div>
              <span className="text-white font-bold text-lg">
                Fremont <span className="text-amber-400">Construction</span>
              </span>
            </div>
            <p className="text-slate-400 text-sm leading-relaxed">
              Quality craftsmanship for residential exterior projects. Licensed,
              insured, and proud to serve the Fremont community.
            </p>
          </div>

          {/* Quick links column */}
          <div>
            <h4 className="text-white font-semibold mb-4">Quick Links</h4>
            <ul className="space-y-2.5">
              {[
                { label: "Services", href: "#services" },
                { label: "Why Us", href: "#why-us" },
                { label: "Contact", href: "#contact" },
                { label: "Get an Estimate", href: "#contact" },
              ].map((link) => (
                <li key={link.label}>
                  <a
                    href={link.href}
                    className="text-slate-400 hover:text-amber-400 text-sm transition-colors duration-200"
                  >
                    {link.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>

          {/* Contact column */}
          <div>
            <h4 className="text-white font-semibold mb-4">Service Area</h4>
            <p className="text-slate-400 text-sm leading-relaxed mb-4">
              Proudly serving{" "}
              <span className="text-slate-300 font-medium">Fremont</span> and
              surrounding areas including the greater metropolitan region.
            </p>
            <div className="space-y-2">
              <div className="flex items-center gap-2 text-slate-400 text-sm">
                <Phone className="w-4 h-4 text-amber-500" />
                (555) 123-4567
              </div>
              <div className="flex items-center gap-2 text-slate-400 text-sm">
                <Mail className="w-4 h-4 text-amber-500" />
                info@fremontcontracting.com
              </div>
            </div>
          </div>
        </div>

        {/* Bottom bar */}
        <div className="pt-8 flex flex-col sm:flex-row items-center justify-between gap-3 text-xs text-slate-500">
          <p>© {year} Fremont Construction & Contracting. All rights reserved.</p>
          <p>Licensed General Contractor · Fully Insured</p>
        </div>
      </div>
    </footer>
  );
}

// ─── Root Page Export ─────────────────────────────────────────────────────────

export default function Page() {
  return (
    <main>
      <Navbar />
      <Hero />
      <Services />
      <WhyChooseUs />
      <Contact />
      <Footer />
    </main>
  );
}
